#! /usr/bin/env node

const customHelp = require('./_/customHelp');
const parseBootstrapOptions = require('./_/parseBootstrapOptions');
const bootstrap = require('../bootstrap');

const options = {};
const program = parseBootstrapOptions(options);

// Custom `--help`
customHelp({ program, binary: 'monopod-bootstrap' });

bootstrap(options);
