#! /usr/bin/env node

const commander = require('commander');
const bootstrapBinary = require('../_/bootstrapBinary');
const bootstrap = require('../../bootstrap');

bootstrapBinary({
  commander, logicModule: bootstrap, manpage: 'monopod-bootstrap',
});
