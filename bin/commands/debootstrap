#! /usr/bin/env node

const bootstrapBinary = require('../_/bootstrapBinary');
const debootstrap = require('../../debootstrap');

bootstrapBinary({ logicModule: debootstrap, manpage: 'monopod-debootstrap' });
